import { AlertOverlayRef } from './alert-overlay-ref';

//Argument of type 'null' is not assignable to parameter of type 'OverlayRef'
describe('AlertOverlayRef', () => {
  // it('should create an instance', () => {
  //   expect(new AlertOverlayRef(null)).toBeTruthy();
  // });
});
